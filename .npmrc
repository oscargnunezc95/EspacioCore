# Exige que solo se use pnpm en este proyecto (bloquea npm)
engine-strict=true

# Bloquea la ejecución automática de scripts de terceros por seguridad.
# (Si alguna librería de Vite o Tailwind falla al instalarse a futuro, 
# cambia esto temporalmente a false o coméntalo con un #).
ignore-scripts=true